<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.reghao.im.db.mapper.ChatGroupMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into chat_group
        (`id`,`deleted`,`create_time`,`update_time`,`group_id`,`name`,`avatar`,`profile`,`owner_id`)
        values 
        (#{id},#{deleted},#{createTime},#{updateTime},#{groupId},#{name},#{avatar},#{profile},#{ownerId})
    </insert>

    <select id="findByGroupId" resultType="cn.reghao.im.model.po.contact.ChatGroup">
        select * from chat_group where id=#{groupId}
    </select>
    <select id="findByGroupIds" resultType="cn.reghao.im.model.po.contact.ChatGroup">
        select * from chat_group where id in (select group_id from chat_group_member where user_id=#{userId})
    </select>
    <select id="findUserIdsByGroupId" resultType="java.lang.Long">
        select user_id from chat_group_member where group_id=#{groupId}
    </select>
</mapper>
